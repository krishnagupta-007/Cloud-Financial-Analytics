# Prometheus Configuration File
global:
  scrape_interval: 5s # Scrape targets every 5 seconds

scrape_configs:
  - job_name: "cloud-financial-analytics"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app:8080"] # 'app' is the service name of Go application in Docker Compose
